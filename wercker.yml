box: wercker/nodejs
build:
  steps:
    - script:
        name: install local dependencies
        code: npm install
    - script:
        name: run tests
        code: npm test
deploy:
  steps:
    - s3sync:
        key-id: $KEY
        key-secret: $SECRET
        bucket-url: $BUCKET
        source-dir: ./dist