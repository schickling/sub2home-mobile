box: wercker/nodejs
build:
  steps:
    - script:
        name: install local dependencies
        code: npm install
    - script:
        name: build
        code: npm run-script build
deploy:
  steps:
    - schickling/s3-website:
        key: $KEY
        secret: $SECRET
        bucket: $BUCKET
        source-dir: ./dist
