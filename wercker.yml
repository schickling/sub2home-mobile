box: wercker/nodejs
build:
  steps:
    - script:
        name: install local dependencies
        code: npm install
    - script:
        name: build
        code: npm run-script build
deploy:
  steps:
    - s3sync:
        key-id: $KEY
        key-secret: $SECRET
        bucket-url: $BUCKET
        source-dir: ./dist
